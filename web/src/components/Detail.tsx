'use client';
import {useCallback,useEffect,useRef,useState,type FormEvent,type ReactNode} from 'react';
import Link from 'next/link';
import {ArrowLeft,ChevronRight,Download,FileText,Folder,RefreshCw,Upload} from 'lucide-react';
import {API,request,json,items,fmtDate,fmtSize,type Server,type ServerPermission,type Template,type Job} from '@/lib/api';
import LiveConsole from '@/components/LiveConsole';
import BackupManager from '@/components/BackupManager';
function Err({text}:{text:string}){return text?<div role="alert" className="notice error">{text}</div>:null}
function Btn({children,...props}:React.ButtonHTMLAttributes<HTMLButtonElement>){return <button {...props} className={`btn ${props.className||''}`}>{children}</button>}
function useData<T>(path:string|null,interval=0){const [data,setData]=useState<T|null>(null),[error,setError]=useState(''),[busy,setBusy]=useState(true),sequence=useRef(0);const reload=useCallback(async()=>{if(!path)return;const ticket=++sequence.current;try{const value=await request<T>(path);if(sequence.current===ticket){setData(value);setError('')}}catch(e){if(sequence.current===ticket){setData(null);setError((e as Error).message)}}finally{if(sequence.current===ticket)setBusy(false)}},[path]);useEffect(()=>{++sequence.current;setBusy(true);setData(null);setError('');reload();const timer=interval?setInterval(reload,interval):null;return()=>{++sequence.current;if(timer)clearInterval(timer)}},[reload,interval]);return {data,error,busy,reload}}
function Submit({children,onRun,label='Save'}:{children:ReactNode;onRun:(v:Record<string,string>)=>Promise<void>;label?:string}){const [pending,setPending]=useState(false),[message,setMessage]=useState(''),[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);setError('');setMessage('');try{await onRun(Object.fromEntries(new FormData(e.currentTarget).entries()) as Record<string,string>);setMessage('Saved / job queued.')}catch(ex){setError((ex as Error).message)}finally{setPending(false)}}return <form className="form" onSubmit={submit}>{children}<Err text={error}/><div className="form-actions"><Btn className="primary" disabled={pending} type="submit">{pending?'Please wait…':label}</Btn>{message&&<span role="status" className="success-text">{message}</span>}</div></form>}
const tabs:Array<{key:string;label:string;permission:ServerPermission}>=[{key:'console',label:'Console',permission:'console'},{key:'files',label:'Files',permission:'files'},{key:'backups',label:'Backups',permission:'backups'},{key:'schedules',label:'Schedules',permission:'manage'},{key:'access',label:'Access',permission:'manage'},{key:'settings',label:'Settings',permission:'manage'}];
export default function ServerDetail({id,admin,actorId,tab}:{id:string;admin:boolean;actorId:string;tab:string}){const {data:s,error,busy,reload}=useData<Server>(`/servers/${id}`,12000);const isOwner=s?.ownerId===actorId;const effectivePermissions=new Set<ServerPermission>(s?.effectivePermissions||[]);const canManage=admin||isOwner||effectivePermissions.has('manage');const canAccess=(permission:ServerPermission)=>canManage||effectivePermissions.has(permission);const visibleTabs=tabs.filter(({permission})=>canAccess(permission));const activeTab=visibleTabs.some(t=>t.key===tab)?tab:visibleTabs[0]?.key??null;const {data:jobs,error:jobsError,busy:jobsBusy,reload:reloadJobs}=useData<Job[]>(`/jobs?serverId=${id}&limit=20`,8000);const [actionError,setActionError]=useState(''),[actionMessage,setActionMessage]=useState(''),[acting,setActing]=useState('');async function action(kind:string,extra:Record<string,unknown>={}){if(['kill','reinstall','restore'].includes(kind)&&!confirm(kind==='restore'?'Restore replaces all files on this server. Continue?':kind==='reinstall'?'Reinstall deletes all local files. Continue?':'Force stop the server without a clean shutdown?'))return;setActing(kind);setActionError('');setActionMessage('');try{const result=await json('POST',`/servers/${id}/actions`,{action:kind,...extra,...(['restore','reinstall'].includes(kind)?{confirm:true}:{})}) as Record<string,any>;setActionMessage(`Job ${result.jobId||result.job?.id||result.id} queued.`);reload();reloadJobs()}catch(e){setActionError((e as Error).message)}finally{setActing('')}}return <><Link href="/servers" className="back"><ArrowLeft size={16}/> All servers</Link>{busy?<div className="skeleton"/>:null}<Err text={error}/>{s&&<><div className="detail-head"><div><div className="eyebrow">SERVER / {s.location}</div><h1>{s.name}</h1><div className="detail-sub"><span className={`badge ${s.status==='running'?'good':s.status==='unreachable'?'bad':'neutral'}`}>{s.status==='unreachable'?'Node unreachable':s.status==='running'?'Running':s.status==='stopped'?'Stopped':s.status}</span><span>{s.nodeName} · Port {s.port} · {s.templateId}</span></div></div>{canManage&&<div className="actions"><Btn disabled={!!acting||s.status==='unreachable'||s.suspended} onClick={()=>action('start')}>Start</Btn><Btn disabled={!!acting||s.status==='unreachable'||s.suspended} onClick={()=>action('restart')}>Restart</Btn><Btn disabled={!!acting||s.status==='unreachable'||s.suspended} onClick={()=>action('stop')}>Stop</Btn><Btn className="subtle-danger" disabled={!!acting||s.status==='unreachable'||s.suspended} onClick={()=>action('kill')}>Force stop</Btn></div>}</div>{s.status==='unreachable'&&<div className="notice error">The node is unreachable. The last observed server state was „{s.observedStatus}“. The server may still be running. Actions are not recommended until the node reconnects.</div>}{s.suspended&&<div className="notice">This server is suspended. Customers cannot start it.</div>}<Err text={actionError}/>{actionMessage&&<div role="status" className="notice">{actionMessage}</div>}<div className="detail-stats"><div><span>MEMORY</span><strong>{s.memoryMb} MB</strong></div><div><span>CPU LIMIT</span><strong>{s.cpuPercent}%</strong></div><div><span>DISK / ALLOWANCE</span><strong>{s.usage?.diskBytes!==undefined?`${(s.usage.diskBytes/1048576).toFixed(0)} / `:''}{s.diskMb} MB</strong></div><div><span>STATUS</span><strong>{s.status}</strong></div></div><nav className="tabs" aria-label="Server sections">{visibleTabs.map(({key,label})=><Link key={key} href={`/servers/${id}?tab=${key}`} className={activeTab===key?'selected':''} aria-current={activeTab===key?'page':undefined}>{label}</Link>)}</nav><div className="tab-panel">{activeTab==='files'?<Files id={id}/>:activeTab==='backups'?<BackupManager id={id} canManage={canManage} canRestore={canManage}/>: activeTab==='schedules'?<Schedules id={id} minecraft={!!s.supportsRcon}/>:activeTab==='access'?<Access id={id}/>:activeTab==='settings'?<ServerSettings id={id} server={s} admin={admin} reload={reload} action={action}/>:activeTab==='console'?<LiveConsole id={id} minecraft={!!s.supportsRcon} reachable={s.status!=='unreachable'}/>:<div className="notice">This server is available in read-only mode. Console, files, backups, or management require the corresponding permission.</div>}</div><section className="section"><div className="section-title"><div><h2>Recent jobs</h2><p className="muted">Refreshes every eight seconds</p></div><Btn onClick={reloadJobs}><RefreshCw size={15}/> Refresh</Btn></div><Err text={jobsError}/>{jobsBusy?<div className="skeleton"/>:jobsError?null:!items(jobs).length?<div className="empty">No jobs found.</div>:<div className="table-wrap"><table><thead><tr><th>Job</th><th>Status</th><th>Created</th><th>Error</th></tr></thead><tbody>{items(jobs).map(j=><tr key={j.id}><td>{j.kind}<div className="muted small mono">{j.id}</div></td><td><span className={`badge ${j.state==='succeeded'?'good':j.state==='failed'?'bad':'neutral'}`}>{j.state}</span></td><td>{fmtDate(j.createdAt)}</td><td className="danger-text">{j.error||'—'}</td></tr>)}</tbody></table></div>}</section></>}</>}
function Files({id}:{id:string}){const [sftp,setSftp]=useState<{username:string;password:string}|null>(null);const [path,setPath]=useState('/'),[selected,setSelected]=useState(''),[content,setContent]=useState(''),[message,setMessage]=useState(''),[opError,setOpError]=useState(''),[uploading,setUploading]=useState(false);const {data,error,busy,reload}=useData<{path:string;items:Array<{name:string;path:string;type:string;size:number;modifiedAt:string}>}>(`/servers/${id}/files?path=${encodeURIComponent(path)}`);async function open(file:string){setSelected('');setContent('');try{const r=await request<{content:string}>(`/servers/${id}/files/content?path=${encodeURIComponent(file)}`);setContent(r.content);setSelected(file);setOpError('')}catch(e){setOpError((e as Error).message)}}async function execute(fn:()=>Promise<any>){setOpError('');setMessage('');try{await fn();setMessage('Operation completed.');reload()}catch(e){setOpError((e as Error).message)}}const parent=path==='/'?'/':path.slice(0,path.lastIndexOf('/'))||'/';return <section className="section"><div className="section-title"><div><h2>Files</h2><p className="muted">Virtual server directory · streamed transfers up to 1 GiB · text editor up to 1 MiB.</p></div><Btn onClick={reload}><RefreshCw size={15}/> Refresh</Btn></div><div className="sftp-access"><Btn onClick={()=>execute(async()=>setSftp(await json('POST',`/servers/${id}/sftp`)))}>Connect with SFTP</Btn>{sftp&&<div className="notice token-notice"><strong>SFTP credentials · valid for 15 minutes</strong><span>Connect to your node’s SFTP address. Local evaluation: localhost:2022.</span><span>Username</span><code>{sftp.username}</code><span>Password</span><code>{sftp.password}</code><Btn onClick={()=>setSftp(null)}>Hide credentials</Btn></div>}</div><div className="breadcrumbs"><Btn disabled={path==='/'} onClick={()=>{setPath(parent);setSelected('')}}>↰ Back</Btn><span className="mono">{path}</span></div><Err text={error||opError}/>{message&&<div role="status" className="notice">{message}</div>}{busy?<div className="skeleton"/>:error?null:!data?.items?.length?<div className="empty">Directory is empty.</div>:<div className="file-list">{data.items.map(f=><div key={f.path} className="file-row"><button disabled={f.type!=='directory'&&f.size>1024*1024} title={f.type!=='directory'&&f.size>1024*1024?'Editor limit is 1 MiB — download file instead':undefined} onClick={()=>f.type==='directory'?(setPath(f.path),setSelected('')):open(f.path)}>{f.type==='directory'?<Folder size={18}/>:<FileText size={18}/>}<span>{f.name}</span></button><span className="muted small">{f.type==='directory'?'Folder':fmtSize(f.size)}{f.type!=='directory'&&f.size>1024*1024?' · download only':''}</span>{f.type!=='directory'&&<><Btn onClick={async()=>{try{const transfer=await json('POST',`/servers/${id}/transfers`,{direction:'download',path:f.path});setMessage('Preparing download…');await waitTransfer(id,transfer.id);const a=document.createElement('a');a.href=`${API}/api/servers/${id}/transfers/${transfer.id}/content`;a.download=f.name;a.click();setMessage('Download ready.');}catch(e){setOpError((e as Error).message)}}} aria-label={`${f.name} download`}><Download size={15}/></Btn>{f.name.toLowerCase().endsWith('.zip')&&<Btn onClick={()=>{if(confirm(`${f.name} extract into this directory?`))execute(()=>json('POST',`/servers/${id}/files/extract`,{path:f.path}))}}>Extract ZIP</Btn>}</>}</div>)}</div>}{!busy&&!error&&<div className="two-col file-tools"><div><h3>Upload</h3><form className="form" onSubmit={async e=>{e.preventDefault();const file=new FormData(e.currentTarget).get('file') as File;setUploading(true);await execute(async()=>{if(file.size>1024**3)throw new Error('Maximum file size is 1 GiB.');const transfer=await json('POST',`/servers/${id}/transfers`,{direction:'upload',path:(path==='/'?'':path)+'/'+file.name,size:file.size});await request(`/servers/${id}/transfers/${transfer.id}`,{method:'PUT',headers:{'Content-Type':'application/octet-stream'},body:file});setMessage('Upload received. Saving on your server…');await waitTransfer(id,transfer.id);});setUploading(false)}}><input aria-label="Choose file" type="file" name="file" required/><Btn disabled={uploading} type="submit"><Upload size={15}/> {uploading?'Uploading…':'Upload file'}</Btn></form></div><div><h3>Create folder</h3><Submit label="Create folder" onRun={async v=>{await json('POST',`/servers/${id}/files/mkdir`,{path:(path==='/'?'':path)+'/'+v.name});reload()}}><label className="field"><span>Folder name</span><input name="name" required pattern="[^/]+"/></label></Submit></div></div>}{selected&&<div className="editor"><div className="section-title"><h3 className="mono">{selected}</h3><Btn onClick={()=>setSelected('')}>Close</Btn></div><textarea aria-label="File contents" value={content} onChange={e=>setContent(e.target.value)} rows={16}/><Btn className="primary" onClick={()=>{if(new TextEncoder().encode(content).length>1024*1024){setOpError('Editor limit: 1 MiB (UTF-8).');return;}execute(()=>json('PUT',`/servers/${id}/files/content?path=${encodeURIComponent(selected)}`,{content}));}}>Save file</Btn></div>}</section>}
function Backups({id,action,canRestore}:{id:string;action:(kind:string,extra?:Record<string,unknown>)=>Promise<void>;canRestore:boolean}){const {data,error,busy,reload}=useData<Array<Record<string,any>>>(`/servers/${id}/backups`,12000);const [opError,setOpError]=useState(''),[message,setMessage]=useState('');async function run(fn:()=>Promise<any>){setOpError('');try{const r=await fn();setMessage(`Job ${r?.jobId||r?.id||''} queued.`);reload()}catch(e){setOpError((e as Error).message)}}return <section className="section"><div className="section-title"><div><h2>Backups</h2><p className="muted">Off-node storage must be configured by an administrator. Backups are live archives.</p></div>{!busy&&!error&&<Btn className="primary" onClick={()=>run(()=>json('POST',`/servers/${id}/backups`))}>Create backup</Btn>}</div><Err text={error||opError}/>{message&&<div className="notice" role="status">{message}</div>}{busy?<div className="skeleton"/>:error?null:!items(data).length?<div className="empty">No backups found.</div>:<div className="table-wrap"><table><thead><tr><th>Created</th><th>Status</th><th>Size</th><th>Actions</th></tr></thead><tbody>{items(data).map(b=><tr key={b.id}><td>{fmtDate(b.createdAt)}<div className="muted small mono">{b.id}</div></td><td>{b.state}{b.error&&<div className="danger-text small">{b.error}</div>}</td><td>{fmtSize(b.sizeBytes)}</td><td className="row-actions">{b.state==='succeeded'&&<><a className="btn" href={`${API}/api/servers/${id}/backups/${b.id}/download`} target="_blank" rel="noreferrer">Download</a>{canRestore&&<Btn onClick={()=>action('restore',{backupId:b.id})}>Restore</Btn>}</>}{!['queued','running'].includes(b.state)&&<Btn className="subtle-danger" onClick={async()=>{if(confirm('Permanently delete this backup?')){setOpError('');setMessage('');try{await request(`/servers/${id}/backups/${b.id}`,{method:'DELETE'});setMessage('Backup deleted.');reload();}catch(e){setOpError((e as Error).message)}}}}>Delete</Btn>}</td></tr>)}</tbody></table></div>}</section>}
function Schedules({id,minecraft}:{id:string;minecraft:boolean}){const {data,error,busy,reload}=useData<Array<Record<string,any>>>(`/servers/${id}/schedules`);const [opError,setOpError]=useState('');return <section className="section"><div className="section-title"><div><h2>Schedules</h2><p className="muted">Interval-based; runs at least every five minutes.</p></div></div>{!busy&&!error&&<Submit label="Create schedule" onRun={async v=>{await json('POST',`/servers/${id}/schedules`,{kind:v.kind,intervalMinutes:Number(v.intervalMinutes),...(v.kind==='command'?{command:v.command}:{})});reload()}}><div className="form-grid"><label className="field"><span>Action</span><select name="kind"><option value="backup">Backup</option>{minecraft&&<option value="command">Minecraft command</option>}</select></label><label className="field"><span>Interval (minutes)</span><input name="intervalMinutes" type="number" min="5" defaultValue="60" required/></label><label className="field"><span>Command (only for the command action)</span><input name="command" placeholder="say Maintenance in 5 minutes"/></label></div></Submit>}<Err text={error||opError}/>{busy?<div className="skeleton"/>:error?null:!items(data).length?<div className="empty">No schedules found.</div>:<div className="table-wrap"><table><thead><tr><th>Type</th><th>Interval</th><th>Next run</th><th>Actions</th></tr></thead><tbody>{items(data).map(t=><tr key={t.id}><td>{t.kind}{t.command&&<div className="muted small mono">{t.command}</div>}</td><td>{t.interval_minutes} min</td><td>{fmtDate(t.next_run_at)}</td><td><Btn className="subtle-danger" onClick={async()=>{if(!confirm('Delete this schedule?'))return;try{await request(`/servers/${id}/schedules/${t.id}`,{method:'DELETE'});reload()}catch(e){setOpError((e as Error).message)}}}>Remove</Btn></td></tr>)}</tbody></table></div>}</section>}
function Access({id}:{id:string}){const {data,error,busy,reload}=useData<Array<Record<string,any>>>(`/servers/${id}/collaborators`);const [opError,setOpError]=useState('');return <section className="section"><div className="section-title"><div><h2>Collaborators</h2><p className="muted">Only existing customer accounts can be added. No email invitation is sent.</p></div></div>{!busy&&!error&&<Submit label="Grant access" onRun={async v=>{const perms=['view','console','files','backups','manage'].filter(p=>v[p]);await json('POST',`/servers/${id}/collaborators`,{email:v.email,permissions:perms});reload()}}><label className="field"><span>Email of an existing customer</span><input name="email" type="email" required/></label><div className="checks">{['view','console','files','backups','manage'].map((p,i)=><label key={p}><input type="checkbox" name={p} defaultChecked={i===0}/> {({view:'View',console:'Console',files:'Files',backups:'Backups',manage:'Manage'} as Record<string,string>)[p]}</label>)}</div></Submit>}<Err text={error||opError}/>{busy?<div className="skeleton"/>:error?null:!items(data).length?<div className="empty">No collaborators added yet.</div>:<div className="table-wrap"><table><thead><tr><th>Customer</th><th>Permissions</th><th>Actions</th></tr></thead><tbody>{items(data).map(c=><tr key={c.userId}><td>{c.email}</td><td>{c.permissions.join(', ')}</td><td><Btn className="subtle-danger" onClick={async()=>{if(!confirm(`Remove access for ${c.email}?`))return;try{await request(`/servers/${id}/collaborators/${c.userId}`,{method:'DELETE'});reload()}catch(e){setOpError((e as Error).message)}}}>Remove</Btn></td></tr>)}</tbody></table></div>}</section>}
function DeleteServer({id}:{id:string}){const [busy,setBusy]=useState(false),[error,setError]=useState(''),[message,setMessage]=useState('');async function remove(){if(!confirm('Permanently delete this server and all local data? This cannot be undone.'))return;setBusy(true);setError('');setMessage('');try{const {jobId}=await request<{jobId:string}>(`/servers/${id}?confirm=true`,{method:'DELETE'});setMessage('Deletion queued. Waiting for the node to remove the server…');for(let attempt=0;attempt<25;attempt++){const jobs=items(await request<Job[]>(`/jobs?serverId=${id}&limit=20`));const job=jobs.find(j=>j.id===jobId);if(job?.state==='failed')throw new Error(job.error||'The node could not delete this server.');if(job?.state==='succeeded'){window.location.assign('/servers');return;}await new Promise(r=>setTimeout(r,1000));}setMessage('Deletion is still queued. Check Recent jobs; the server will disappear after the node confirms removal.');}catch(e){setError((e as Error).message)}finally{setBusy(false)}}return <div className="delete-server"><Btn className="subtle-danger" disabled={busy} onClick={remove}>{busy?'Deleting…':'Delete server'}</Btn><Err text={error}/>{message&&<div role="status" className="notice">{message}</div>}</div>}
function ServerSettings({id,server,admin,reload,action}:{id:string;server:Server;admin:boolean;reload:()=>void;action:(kind:string,extra?:Record<string,unknown>)=>Promise<void>}){const {data:templates,error:templateError,busy:templateBusy}=useData<Template[]>('/templates');const editable=items(templates).find(t=>t.id===server.templateId)?.editableVariables||[];return <><section className="section"><div className="section-title"><div><h2>Startup variables</h2><p className="muted">Changing these values recreates the container with its existing data and causes downtime. Leave a field blank to keep its current value.</p></div></div><Err text={templateError}/>{templateBusy?<div className="skeleton"/>:templateError?null:editable.length?<Submit label="Update startup variables" onRun={async v=>{const variables=Object.fromEntries(editable.filter(k=>v[k]!==undefined&&(server.variables?.[k]===undefined?v[k]!=='':v[k]!==server.variables[k])).map(k=>[k,v[k]]));if(!Object.keys(variables).length)throw new Error('No changes to save.');await json('PATCH',`/servers/${id}/settings`,{variables});reload()}}><div className="form-grid">{editable.map(key=><label className="field" key={key}><span>{key}</span><input name={key} defaultValue={server.variables?.[key]||''}/></label>)}</div></Submit>:<div className="empty">This template has no editable variables.</div>}</section>{admin&&<section className="section"><div className="section-title"><div><h2>Administrative actions</h2><p className="muted">Resource changes are checked against reserved capacity.</p></div></div><Submit label="Update server" onRun={async v=>{await json('PATCH',`/servers/${id}`,{name:v.name,memoryMb:Number(v.memoryMb),cpuPercent:Number(v.cpuPercent),diskMb:Number(v.diskMb)});reload()}}><div className="form-grid"><label className="field"><span>Name</span><input name="name" defaultValue={server.name} required/></label><label className="field"><span>RAM (MB)</span><input name="memoryMb" type="number" min="1" defaultValue={server.memoryMb} required/></label><label className="field"><span>CPU (%)</span><input name="cpuPercent" type="number" min="1" defaultValue={server.cpuPercent} required/></label><label className="field"><span>Disk (MB)</span><input name="diskMb" type="number" min="1" defaultValue={server.diskMb} required/></label></div></Submit><div className="danger-zone"><Btn onClick={()=>action(server.suspended?'unsuspend':'suspend')}>{server.suspended?'Unsuspend':'Suspend'}</Btn><Btn className="subtle-danger" onClick={()=>action('reinstall')}>Reinstall</Btn><DeleteServer id={id}/></div></section>}</>}

async function waitTransfer(serverId:string,id:string){const end=Date.now()+30*60*1000;while(Date.now()<end){const t=await request<{state:string;error?:string}>(`/servers/${serverId}/transfers/${id}`);if(t.state==='succeeded')return;if(t.state==='failed')throw new Error(t.error||'Transfer failed');await new Promise(r=>setTimeout(r,2000));}throw new Error('Transfer is still pending. Check recent jobs.');}
