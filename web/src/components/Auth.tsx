'use client';
import {useEffect,useRef,useState,type FormEvent} from 'react';
import {ArrowUpRight,LoaderCircle,ShieldCheck,X} from 'lucide-react';
import {json,request,type User} from '@/lib/api';

export function AsciiField(){
 const art=Array.from({length:44},(_,y)=>Array.from({length:84},(_,x)=>{
  const dx=(x-42)/2,dy=y-22,r=Math.sqrt(dx*dx+dy*dy),wave=Math.sin(r*.7+Math.atan2(dy,dx)*3);
  return r<5?' ':wave>.8?'@':wave>.35?'+':wave>-.1?':':wave>-.6?'.':' ';
 }).join('')).join('\n');
 return <pre className="ascii-field" aria-hidden="true">{art}</pre>;
}

export default function Auth({existing,onSuccess}:{existing?:User;onSuccess:(u:User)=>void}){
 const [mode,setMode]=useState<'loading'|'login'|'bootstrap'|'setup'|'recover'>(existing?'setup':'loading');
 const [error,setError]=useState(''),[busy,setBusy]=useState(false),[secret,setSecret]=useState(''),[uri,setUri]=useState(''),[challenge,setChallenge]=useState('');
 const dialog=useRef<HTMLDialogElement>(null),started=useRef(false);
 async function setup(){const s=await json('POST','/auth/2fa/setup');setSecret(s.secret);setUri(s.uri);setMode('setup');}
 useEffect(()=>{if(started.current)return;started.current=true;if(existing)setup().catch(e=>setError(e.message));else request<{needsSetup:boolean}>('/auth/status').then(s=>setMode(s.needsSetup?'bootstrap':'login')).catch(e=>setError(e.message));},[existing]);
 useEffect(()=>{if(challenge)dialog.current?.showModal();else dialog.current?.close();},[challenge]);
 async function complete(){const me=await request<User>('/auth/me');if(me.role==='admin'&&!me.has2fa)await setup();else onSuccess(me);}
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');setBusy(true);const f=Object.fromEntries(new FormData(e.currentTarget));try{
  if(mode==='recover'){await json('POST','/auth/recover',{email:f.email,password:f.password,recoveryCode:f.recoveryCode});setMode('login');setError('Account recovered. Sign in with your new password and configure your authenticator again.');return;}
  if(mode==='setup'){await json('POST','/auth/2fa/confirm',{code:f.code});await complete();}
  else {const r=await json('POST',`/auth/${mode}`,{email:f.email,password:f.password,stepwise:true});if(r.requires2fa)setChallenge(r.challenge);else await complete();}
 }catch(e){setError((e as Error).message);}finally{setBusy(false);}}
 async function verify(e:FormEvent<HTMLFormElement>){e.preventDefault();setError('');setBusy(true);try{await json('POST','/auth/challenge',{challenge,code:new FormData(e.currentTarget).get('code')});setChallenge('');await complete();}catch(e){setError((e as Error).message);}finally{setBusy(false);}}
 const title=mode==='recover'?'Find your way back.':mode==='bootstrap'?'Make it yours.':mode==='setup'?'One more layer.':'Welcome back.';
 return <main className="auth-layout"><section className="auth-left"><a className="auth-logo" href="/"><img src="/fledge-symbol.png" alt=""/> Fledge<span>CONTROL PANEL</span></a><div className="auth-form-wrap"><div className="eyebrow">{mode==='bootstrap'?'01 / FIRST ADMINISTRATOR':mode==='setup'?'02 / PROTECT YOUR ACCOUNT':'YOUR INFRASTRUCTURE, CONNECTED'}</div><h1>{title}</h1><p className="auth-intro">{mode==='recover'?'Use a saved recovery code to reset your access.':mode==='bootstrap'?'Create the first administrator to open your workspace.':mode==='setup'?'Connect an authenticator app to keep your workspace secure.':'Sign in. Your servers are waiting.'}</p>{mode==='loading'?<div className="loading-state"><LoaderCircle className="spin"/><span>{error||'Opening your workspace…'}</span>{error&&<button className="btn" onClick={()=>location.reload()}>Retry</button>}</div>:<form onSubmit={submit} className="form">{mode==='setup'?<><div className="secret"><span>Authenticator setup key</span><code>{secret||'Loading…'}</code></div><a className="link" href={uri}>Open in your authenticator <ArrowUpRight size={14}/></a><label className="field"><span>Verification code</span><input name="code" required inputMode="numeric" pattern="[0-9]{6}" maxLength={6} autoComplete="one-time-code" placeholder="000000"/></label></>:<><label className="field"><span>Email address</span><input name="email" type="email" required autoComplete="username" placeholder="you@yourcompany.com"/></label><label className="field"><span>{mode==='recover'?'New password':'Password'}</span><input name="password" type="password" required minLength={mode!=='login'?12:undefined} autoComplete={mode!=='login'?'new-password':'current-password'} placeholder={mode!=='login'?'At least 12 characters':'Enter your password'}/></label>{mode==='recover'&&<label className="field"><span>Saved recovery code</span><input name="recoveryCode" required autoComplete="off"/></label>}</>}{error&&!challenge&&<p className="notice error" role="alert">{error}</p>}<button className="btn primary auth-submit" disabled={busy||mode==='setup'&&!secret}>{busy?<LoaderCircle size={18} className="spin"/>:null}{mode==='recover'?'Reset account access':mode==='bootstrap'?'Create workspace':mode==='setup'?'Enable protection':'Continue'}<ArrowUpRight size={18}/></button><div className="auth-secure"><ShieldCheck size={14}/> {mode==='login'?'Two-factor verification follows sign-in.':'Your workspace starts with secure access.'}</div></form>}{(mode==='login'||mode==='recover')&&<button className="text-button recovery-link" onClick={()=>{setMode(mode==='login'?'recover':'login');setError('');}}>{mode==='login'?'Lost access? Use a recovery code':'Back to sign in'}</button>}</div><footer className="auth-bottom"><a href="/install">Getting started</a><span>Fledge / Infrastructure, simplified.</span><span>© {new Date().getFullYear()}</span></footer></section><aside className="auth-art"><AsciiField/><div className="art-top"><span>BUILT FOR YOUR NEXT WORLD</span><span>↗</span></div><h2>Less friction.<br/>More play.</h2><div className="art-caption"><span className="art-symbol">✳</span><p>Your servers.<br/>One place to make it all happen.</p><span className="mono">N / 001</span></div></aside><dialog ref={dialog} className="auth-dialog" onCancel={()=>{setChallenge('');setError('');}} aria-labelledby="verification-title"><button className="dialog-close" aria-label="Close verification" onClick={()=>{setChallenge('');setError('');}}><X size={20}/></button><ShieldCheck size={28}/><div className="eyebrow">SECOND STEP / VERIFICATION</div><h2 id="verification-title">Just making sure it’s you.</h2><p className="muted">Enter the six-digit code from your authenticator app.</p><form onSubmit={verify} className="form"><label className="field"><span>Verification code</span><input name="code" className="otp-input" required pattern="[0-9]{6}" inputMode="numeric" autoComplete="one-time-code" maxLength={6} placeholder="000000" autoFocus/></label>{error&&<p role="alert" className="notice error">{error}</p>}<button className="btn primary" disabled={busy}>{busy?<LoaderCircle size={16} className="spin"/>:null}Verify and sign in</button></form></dialog></main>;
}
